import { myCustomFont } from '@/lib/fonts';
import '../lib/globals.css';
import '../lib/fontawesome'
import _navbar from '@/components/home_page/navbar';
export const metadata = {
  title: 'TTA Land',
  description: 'Generated by Next.js',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${myCustomFont.className} relative`}>
        <div className='h-full bg-[url(/img/background.png)] bg-cover bg-center bg-no-repeat w-full fixed'></div>
        <div className='absolute top-0'>
          <_navbar/>
          {children}
        </div>
      </body>
    </html>
  )
}
